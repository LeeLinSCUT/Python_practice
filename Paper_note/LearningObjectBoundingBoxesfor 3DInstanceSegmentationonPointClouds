Vac：
 conceptually：从概念上讲 simultaneously：同时 Extensive：广泛  tackle：应对 obstacle:障碍 incur:招致 formulate：规定
 objectness：    explicitly：明确地  inevitably 不可避免地 precisely  恰好
（一）引言
1)anchor：在深度学习时代，物体检测问题通常都被建模成对一些候选区域进行分类和回归的问题。在单阶段检测器中，这些候选区域就是通过滑窗方式产生的 anchor；
在两阶段检测器中，候选区域是 RPN 生成的 proposal，但是 RPN 本身仍然是对滑窗方式产生的 anchor 进行分类和回归。
2)滑窗，anchor-free，anchor-based，bounding box的区别：
3）CNN需要对点云数据进行体素化才行（解决了数据无序化，无结构，不统一的问题？
